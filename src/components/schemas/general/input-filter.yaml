description: |
  This field no longer does anything, as improvements in search have rendered it 
  redundant. This is deprecated.
type: string
enum: [none, loose, strict]
default: loose
deprecated: true