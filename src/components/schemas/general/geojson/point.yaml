type: object
required:
  - coordinates
  - type
properties:
  coordinates:
    type: array
    minItems: 2
    maxItems: 2
    items:
      type: number
      format: double
  type:
    type: string
    enum: [Point]
