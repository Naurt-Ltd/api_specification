type: object
required: [type, features]
properties:
  features:
    type: array
    minItems: 1
    maxItems: 4
    items:
      $ref: "./feature.yaml"
  type:
    type: string
    enum: [FeatureCollection]
    default: FeatureCollection
