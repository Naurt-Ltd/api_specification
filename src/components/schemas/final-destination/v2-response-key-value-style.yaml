type: object
additionalProperties: false
required: [responses, version, request_id]
properties:
  responses:
    type: array
    items:
      $ref: "./v2-search-response-key-value-style.yaml"
  logging:
    $ref: "./v2-logging.yaml"
  version:
    type: string
  request_id:
    type: string
    format: uuid
