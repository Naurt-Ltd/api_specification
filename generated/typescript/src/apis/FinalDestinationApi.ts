/* tslint:disable */
/* eslint-disable */
/**
 * Naurt API
 * OpenAPI specification for Naurt\'s APIs. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  FinalDestinationRequest,
  FinalDestinationResponse,
  OptionsResponse,
} from '../models/index';
import {
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    FinalDestinationRequestFromJSON,
    FinalDestinationRequestToJSON,
    FinalDestinationResponseFromJSON,
    FinalDestinationResponseToJSON,
    OptionsResponseFromJSON,
    OptionsResponseToJSON,
} from '../models/index';

export interface FinaldestinationPostRequest {
    finalDestinationRequest: FinalDestinationRequest;
}

/**
 * 
 */
export class FinalDestinationApi extends runtime.BaseAPI {

    /**
     * Creates request options for finaldestinationOptions without sending the request
     */
    async finaldestinationOptionsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // ApiKeyAuth authentication
        }


        let urlPath = `/final-destination/v2`;

        return {
            path: urlPath,
            method: 'OPTIONS',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Options
     */
    async finaldestinationOptionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OptionsResponse>> {
        const requestOptions = await this.finaldestinationOptionsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OptionsResponseFromJSON(jsonValue));
    }

    /**
     * Options
     */
    async finaldestinationOptions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OptionsResponse> {
        const response = await this.finaldestinationOptionsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for finaldestinationPost without sending the request
     */
    async finaldestinationPostRequestOpts(requestParameters: FinaldestinationPostRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['finalDestinationRequest'] == null) {
            throw new runtime.RequiredError(
                'finalDestinationRequest',
                'Required parameter "finalDestinationRequest" was null or undefined when calling finaldestinationPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["Authorization"] = await this.configuration.apiKey("Authorization"); // ApiKeyAuth authentication
        }


        let urlPath = `/final-destination/v2`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FinalDestinationRequestToJSON(requestParameters['finalDestinationRequest']),
        };
    }

    /**
     * Query the Naurt Final Destination dataset to retrieve precise destination locations for addresses, buildings, entrances, and parking locations.  Each request contains one or more `queries`. A query can represent several different search types supported by the Final Destination API:  • Forward geocoding — supply an `address_string` or `address_structured`   • Reverse geocoding — supply a `location` with latitude and longitude   • Naurt ID lookup — supply an `id` returned from a previous query   • Source ID lookup — supply identifiers such as `os_uprn`    The API returns a best match for each query along with optional additional matches. Results may include multiple destination geometries such as building entrances, delivery doors, or parking locations depending on available data.  The geometry format returned by the API is controlled by `options.geojson_type`:  • `geojson` (default) — results are returned as standard GeoJSON   FeatureCollections with geometries and properties.  • `key_value` — results are returned as a simplified key/value representation   where each destination type contains latitude and longitude coordinates.  Each query result includes a status, the best match, optional additional matches, and metadata such as distance from the search location when applicable. 
     * Post
     */
    async finaldestinationPostRaw(requestParameters: FinaldestinationPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinalDestinationResponse>> {
        const requestOptions = await this.finaldestinationPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinalDestinationResponseFromJSON(jsonValue));
    }

    /**
     * Query the Naurt Final Destination dataset to retrieve precise destination locations for addresses, buildings, entrances, and parking locations.  Each request contains one or more `queries`. A query can represent several different search types supported by the Final Destination API:  • Forward geocoding — supply an `address_string` or `address_structured`   • Reverse geocoding — supply a `location` with latitude and longitude   • Naurt ID lookup — supply an `id` returned from a previous query   • Source ID lookup — supply identifiers such as `os_uprn`    The API returns a best match for each query along with optional additional matches. Results may include multiple destination geometries such as building entrances, delivery doors, or parking locations depending on available data.  The geometry format returned by the API is controlled by `options.geojson_type`:  • `geojson` (default) — results are returned as standard GeoJSON   FeatureCollections with geometries and properties.  • `key_value` — results are returned as a simplified key/value representation   where each destination type contains latitude and longitude coordinates.  Each query result includes a status, the best match, optional additional matches, and metadata such as distance from the search location when applicable. 
     * Post
     */
    async finaldestinationPost(requestParameters: FinaldestinationPostRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinalDestinationResponse> {
        const response = await this.finaldestinationPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
